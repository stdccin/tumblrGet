using System;
using System.Collections.Generic;
using System.Linq;
using System.Text.RegularExpressions;

namespace tumblrCrawler.MediaParse
{
    public partial class Parser
    {
       /* public Parser(string page)
        {
            List<ArchivePageLinkStruct> ls = GetArchivePageToLinkList(page);
            string link = string.Empty;
            foreach (ArchivePageLinkStruct a in ls)
            {
                link += string.Format("Type: {0}\r\nLink: {1}\r\n", a.Type.ToString(), a.PageLink);
            }
            System.IO.File.WriteAllText(@"C:\Users\dan\Desktop\var.text", link);
        }
        */

    }



}
